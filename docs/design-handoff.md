# デザイン引き渡しチェックリスト

最終更新: 2026-06-26  
対象: web-designer / CEO レビュー

---

## 2026-06-26 インシデント（ロゴ小）

**原因:** HTML が `logo-*.png` を参照していたが **`samples/assets/` に PNG が存在しなかった**（404 → alt テキストだけ表示 → 極小に見える）。

**再発防止（必須）:**

1. **画像を置いてから HTML を書く** — パス追加と同コミットでバイナリも入れる
2. **プレビュー後に DevTools → Network** でロゴ 200 を確認
3. **ヘッダーロゴ最小サイズ** — CSS `height` 下限を守る（下表）
4. **完了報告にスクショ** — ヘッダーが切れていない 1 枚を添付

---

## ロゴサイズ規格（日本の政治なう）

| 用途 | ファイル | CSS 高さ |
|------|----------|----------|
| ヘッダー（PC） | `samples/assets/logo-header-nihon-seiji-naw.png` | **10.5rem**（画像高さ・変更しない） |
| ヘッダー帯の余白 | — | **min-height 7.35rem**（旧 11rem の 2/3） |
| ヘッダー（SP） | 同上 | **7.5rem** |
| アイコン単体 | `samples/assets/logo-mark-hiyoko.png` | favicon / OGP のみ |

- ヘッダーは **横長 1 枚**（マーク＋ワードマーク分割は使わない）
- `max-width: min(26rem, 72vw)` — 検索窓を潰さない

---

## デザイナー完了前チェック（5 分）

- [ ] `samples/preview.ps1` → http://localhost:8770/ 全 3 ページ
- [ ] ロゴ PNG が Network 200
- [ ] キャッチは **あの話どうなった？**（旧「今どうなった？」禁止）
- [ ] 検索窓は **ヘッダー 1 つのみ**（トップ hero に検索なし）
- [ ] `/search.html` にカテゴリチップ＋ソート説明
- [ ] 640px 幅でヘッダー崩れなし
- [ ] **案件ページ目次** — 各リンククリックで見出しがヘッダーに隠れない（`scroll-margin`）
- [ ] **フロート目次** — PC でヘッダー直下に配置、スクロール追従の active がずれない
- [ ] **samples/css/main.css との差分** — ヘッダー・目次は正本から勝手に縮めない

---

## デバッガー必須（公開前・案件1件あたり）

| 項目 | 手順 |
|------|------|
| 目次アンカー | フロート目次の全項目をクリック → 見出しが sticky ヘッダーに被らない |
| 目次 active | 手スクロールで active ハイライトが現在セクションと一致 |
| リンク | 404・国会 speechURL・外部出典 |
| 表示 | 375px / 640px / 900px でヘッダー・目次・😊😠 |
| Safe Browsing | Google Transparency Report |

---

## エージェント向け

スキル不足というより **検証手順の欠落**。上記チェックをタスク完了条件に含めること。
