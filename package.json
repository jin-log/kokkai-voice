{
  "name": "kokkai-voice",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "deploy": "npm run build && wrangler pages deploy dist --project-name=kokkai-voice --branch=main"
  },
  "dependencies": {
    "astro": "^5.7.0"
  },
  "devDependencies": {
    "wrangler": "^4.104.0"
  }
}
